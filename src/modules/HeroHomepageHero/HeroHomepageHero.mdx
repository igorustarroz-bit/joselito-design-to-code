import { Meta, Canvas, Controls, Story } from '@storybook/addon-docs/blocks'
import * as HeroHomepageHeroStories from './HeroHomepageHero.stories'

<Meta of={HeroHomepageHeroStories} />

# Hero / Homepage hero

## Intro

`HeroHomepageHero` traduce el módulo **Hero / Homepage hero** de Figma (`node 58182:4353`, COMPONENT_SET: Property1=desktop/mobile). Imagen de fondo a sangre completa, con una barra de navegación superpuesta, un bloque de contenido centrado (label/título/botón) y una tarjeta "Toast" flotante.

Es un módulo "contenedor" (`src/modules/`, no `src/components/`): ocupa el 100% del ancho del viewport.

## Demo

<Canvas of={HeroHomepageHeroStories.Default} />

## Anatomía

1. **Fondo** — imagen a sangre completa (`object-fit: cover`) + degradado inferior (solo mobile).
2. **Navegación** — barra superpuesta con logo centrado, enlaces (desktop) o icono de menú (mobile), buscador, cuenta y cesta.
3. **Contenido central** — label + título (`--text-title-5`) + botón primario, centrados sobre la imagen.
4. **Toast** — tarjeta flotante (imagen + título + descripción) anclada a la esquina inferior.

<Story of={HeroHomepageHeroStories.NoToast} />

## Comportamiento

**Alcance de Navigation y Toast — importante:** el master de Figma de este módulo incrusta, dentro de su propia anatomía, una barra de navegación completa y una tarjeta "Toast". Ambas piezas están además listadas como módulos independientes y todavía `pendiente` en `PLAN.md` ("Navigation (header principal)" y "Toast"). Para no bloquear este módulo ni duplicar ese trabajo por adelantado, aquí se han construido versiones **simplificadas e internas** de ambas piezas — funciones no exportadas dentro de `HeroHomepageHero.jsx`, suficientes para reproducir el diseño visual (enlaces, buscador, cuenta, cesta con contador; tarjeta con imagen + texto). Cuando se construyan los módulos dedicados "Navigation (header principal)" y "Toast" en su turno del plan, sustituir estas piezas internas por instancias de esos componentes reales.

El icono de menú móvil (hamburguesa) es en Figma un asset raster suelto — se sustituye aquí por el icono componentizado `List` del sistema de iconos, para no introducir un asset fuera del pipeline.

La propiedad "Property1" (desktop/mobile) es responsive real (media query en `768px`), igual criterio que el resto de módulos.

**Tema:** el bloque de contenido usa `data-theme="dark-black-neutral"` sobre la imagen de fondo — el label (`--color-content-neutral-3`, `#b3b3b3`) y el título (`--color-content-base`, blanco) de ese tema coinciden exactamente con los valores de Figma. La tarjeta Toast, blanca con texto oscuro, sobreescribe de vuelta a `data-theme="light-white"` en su propio contenedor — mismo patrón de temas anidados del punto 16 de las instrucciones (footer oscuro + bloque hijo claro).

**Grid de 12 columnas:** los márgenes laterales del bloque de contenido usan `padding-inline: var(--grid-margin-default)`. El ancho del bloque de contenido (664px desktop) es literal — a 1440px no coincide limpiamente con ningún nº de columnas (6 columnas = 648px, 16px de diferencia, por encima del redondeo de 4px aceptado en otros módulos), así que se centra con flexbox igual que Hero / Section hero. Los márgenes propios de la barra de navegación (40px desktop / 24px mobile) son distintos y más estrechos que `--grid-margin-default` — literal específico de esa pieza, no se fuerza al token del módulo.

## Tokens

`--text-body-2` (label y textos de nav/toast), `--text-title-5` (título), `--font-body`/`--font-title`, `--tracking-title`, `--color-content-base`/`--color-content-neutral-2`/`--color-content-neutral-3`/`--color-surface-base` (vía temas), `--grid-margin-default`. Botón: `UI02-Button` tipo `primary`, tamaño `s` (48px alto — coincide exactamente con Figma). Alturas de sección (800px/860px) y ancho del bloque de contenido (350px/664px) son literales sin variable en Figma.

## Propiedades

| Nombre | Descripción | Por defecto | Control |
|---|---|---|---|
| `backgroundImage` / `backgroundImageAlt` | Imagen de fondo a sangre completa | — | — |
| `navLinks` | Enlaces de navegación (desktop) | Tienda/Origen/Excelencia/Compromisos/Experiencia | — |
| `label` | Label superior del contenido | `'DICTUM CRAS ERAT'` | text |
| `title` | Título del hero | — | text |
| `buttonText` / `buttonHref` / `onButtonClick` | Texto y destino del botón primario | `'LOREM IPSUM'` | text |
| `showToast` | Muestra/oculta la tarjeta Toast | `true` | boolean |
| `toastImage` / `toastImageAlt` | Imagen de la tarjeta Toast | — | — |
| `toastTitle` / `toastDescription` | Textos de la tarjeta Toast | — | text |

## Accesibilidad

- La navegación usa `<nav aria-label="Navegación principal">`.
- Los iconos de solo-icono (menú, buscar, cesta en mobile) llevan `title` para lector de pantalla vía el prop `title` de `Icon`.
- El botón usa el componente `Button` accesible ya construido (foco visible, `href` opcional como enlace real).

## Componentes relacionados

- **Icon** — iconos `List`, `MagnifyingGlass`, `Bag`.
- **Brand Logo** — logo centrado de la nav.
- **UI02-Button** — botón primario del contenido.
- **Aspect Ratio** — imagen de la tarjeta Toast.
- **Hero / Section hero** — mismo criterio de centrado por flexbox sobre fondo full-bleed.
- **Navigation (header principal)** y **Toast** — módulos pendientes que sustituirán a las piezas internas simplificadas de aquí (ver "Comportamiento").
